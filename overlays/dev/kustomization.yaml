# overlays/dev/kustomization.yaml
resources:
- ../../base  # Reference the base configuration

helmCharts:
  - name: my-app
    repo: file://github.com/gunjana-bohra/helmchart/tree/helm/base/helmchart  # Path to Helm chart in the base directory
    valuesFile: overlays/dev/values.yaml  # Dev-specific values.yaml
