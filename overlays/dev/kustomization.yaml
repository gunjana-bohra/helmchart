# overlays/dev/kustomization.yaml
bases:
  - ../../base/helmchart  # Reference the base configuration

helmCharts:
  - name: my-app
    repo: https://github.com/gunjana-bohra/helmchart/tree/helm/base/helmchart  # Path to Helm chart in the base directory
    valuesFile: overlays/dev/values.yaml  # Dev-specific values.yaml
